"""LMS AI Engine — a local-first, LMS-agnostic engine that turns documents and
media into structured, portable, interactive micro-learning (H5P / SCORM / xAPI).

Runs entirely on self-hosted open models (via Ollama); no external AI APIs by default.
"""

__version__ = "0.1.0"
