"""Module A — document ingestion.

Parsers (PDF, PPT, …) read a source file and return a `ParsedDocument`
(see `schema.py`) — a clean, structured representation that the rest of the
engine builds on.
"""
