"""Module E — one source, one course.

Modules A to D each do one thing. This one runs them together and reports on each,
so a teacher uploads a file once instead of orchestrating five calls in order.
"""
